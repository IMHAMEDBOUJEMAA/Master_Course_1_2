] add Flux Plots ProgressMeter // install package
using Flux // using package

test = Dense (5,4,\sigma+Tab) // Define inputs& outputs

test.weight       // accéder au Matrice
test.weight[2,4]  // accéder a une seule fonction

test.bias         // init

using Plots

rand( value)
histogram
histogram(x, nbins=number)
 histogram2d
 histogram2d(x,y)
 
 lois uniforme 
 lois normale  